<!-- I'm just using this if statement to comment out this multilined erb code -->
<% if false %>

<% provide(:title, 'Edit user') %>
<% provide(:button_text, 'Save changes') %>
<h1>Update Profile</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a href="http://gravatar.com/emails" target="_blank">Change</a>
    </div>
  </div>
</div>

<% end %>

<h3>Editing User</h3>

<div class="panel panel-default">
        <div class="panel-body">
		<%= render 'form', user: @user %>
        </div>
        <div class="panel-footer">

		<%= link_to 'Back', users_path, class:"btn btn-xs btn-default margin-right color-white" %>
		<%= link_to 'Show', @user, class:"btn btn-xs btn-primary color-white"%>
	</div>
</div>
