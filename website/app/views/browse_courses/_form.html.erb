<%= form_for(browse_course) do |f| %>
  <% if browse_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(browse_course.errors.count, "error") %> prohibited this browse_course from being saved:</h2>

      <ul>
      <% browse_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :course_code %>
    <%= f.text_field :course_code, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit :Submit, class:"btn btn-success" %>
  </div>
<% end %>
