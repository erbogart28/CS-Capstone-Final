<%= form_for(course_delivery) do |f| %>
  <% if course_delivery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_delivery.errors.count, "error") %> prohibited this course_delivery from being saved:</h2>

      <ul>
      <% course_delivery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :course_id %>
    <%= f.number_field :course_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :course_term %>
    <%= f.text_field :course_term, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :course_frequency %>
    <%= f.text_field :course_frequency, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit :Submit, class:"btn btn-success" %>
  </div>
<% end %>
