<%= form_for(prereq) do |f| %>
  <% if prereq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prereq.errors.count, "error") %> prohibited this prereq from being saved:</h2>

      <ul>
      <% prereq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :course_id %>
    <%= f.number_field :course_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :prereq_course_id %>
    <%= f.number_field :prereq_course_id, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit :Submit, class:"btn btn-success" %>
  </div>
<% end %>
