<%= form_for(path_course) do |f| %>
  <% if path_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(path_course.errors.count, "error") %> prohibited this path_course from being saved:</h2>

      <ul>
      <% path_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :path_id %>
    <%= f.number_field :path_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :course_id %>
    <%= f.number_field :course_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :year %>
    <%= f.number_field :year, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :course_term %>
    <%= f.text_field :course_term, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit :Submit, class:"btn btn-success" %>
  </div>
<% end %>
